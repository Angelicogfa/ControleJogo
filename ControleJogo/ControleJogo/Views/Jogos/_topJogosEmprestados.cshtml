@model IEnumerable<ControleJogo.Infra.DatabaseRead.OutputModel.TopEmprestados>

<div id="myCarousel" class="carousel slide" data-ride="carousel" data-interval="6000">
    <ol class="carousel-indicators">
        <li data-target="#myCarousel" data-slide-to="0" class="active"></li>
        <li data-target="#myCarousel" data-slide-to="1"></li>
        <li data-target="#myCarousel" data-slide-to="2"></li>
        <li data-target="#myCarousel" data-slide-to="3"></li>
        <li data-target="#myCarousel" data-slide-to="4"></li>
    </ol>
    <div class="carousel-inner" role="listbox">
        @{
            for (int i = 0; i < Model.Count(); i++)
            {
                var item = Model.ElementAt(i);
                <div class="item @(i == 0 ? "active" : string.Empty)">
                    <img src="@Url.Action("ObterFotoJogo", "Jogos", new { Id = item.JogoId })" alt="@($"Jogo {item.NomeJogo}")" class="img-responsive" style="align-content:stretch"/>
                    <div class="carousel-caption" role="option">
                        <h1 style="font-style:oblique">
                            @item.NomeJogo
                        </h1>
                        <p>Total de emprestimos: <h3>@item.QuantidadeEmprestimos</h3></p>
                    </div>
                </div>
            }
        }
    </div>
    <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
        <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
        <span class="sr-only">Anterior</span>
    </a>
    <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
        <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
        <span class="sr-only">Próximo</span>
    </a>
</div>
